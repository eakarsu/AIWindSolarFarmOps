{
  "name": "ai-wind-solar-farm-ops",
  "version": "1.0.0",
  "description": "AI-powered renewable energy operations platform (wind/solar farms, turbine health, curtailment, PPA settlement, weather forecasting)",
  "private": true,
  "scripts": {
    "start": "bash start.sh",
    "backend": "cd backend && npm start",
    "frontend": "cd frontend && npm start",
    "seed": "cd backend && node seed/seed.js",
    "install-all": "npm install && cd backend && npm install && cd ../frontend && npm install"
  }
}
